language: clojure

lein: 2.6.1

cache:
  directories:
    - $HOME/.m2
    
script: 
 - lein do clean, eastwood, bikeshed -m 100, check
 - lein ancient
 - lein cloverage
 
deploy:
  provider: script
  script: scripts/deploy.sh
  on:
    branch: master
